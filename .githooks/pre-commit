#!/bin/sh
# Guards run before every commit. Enable once per clone:
#   git config core.hooksPath .githooks
set -e
cargo fmt --all --check
cargo clippy --workspace --all-targets --locked -- -D warnings
cargo test --workspace --locked --quiet
